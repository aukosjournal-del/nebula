services:
  - type: web
    name: nebula-backend
    env: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm install && npx prisma generate && npm run build
    startCommand: cd backend && node dist/sync.js && node run.mjs
    envVars:
      - key: NODE_ENV
        value: production
