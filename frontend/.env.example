# Dev only
VITE_PORT=8080

# Leave empty in dev (Vite proxy handles /api → localhost:3000)
# In production (Vercel), set this to your Railway backend URL:
# VITE_API_URL=https://your-backend.railway.app
